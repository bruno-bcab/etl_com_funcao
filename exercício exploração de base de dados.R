pacman::p_load(funModeling, tidyverse) 

glimpse(iris) # olhada nos dados
status(iris) # estrutura dos dados (missing etc)
freq(iris) # frequência das variáveis fator
plot_num(iris) # exploração das variáveis numéricas
profiling_num(iris) # estatísticas das variáveis numéricas

plot_num(iris)
library(funModeling)
library(tidyverse)

##EXERCÍCIO EXPLORAÇÃO DE BASE DE DADOS

glimpse(DATARSU) # olhada nos dados
status(DATARSU) # estrutura dos dados (missing etc)
freq(DATARSU) # frequência das variáveis fator
plot_num(iris) # exploração das variáveis numéricas
profiling_num(DATARSU) # estatísticas das variáveis numéricas
plot(DATARSU)
